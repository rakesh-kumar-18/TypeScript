const superHeros: string[] = [];

superHeros.push("iron man");

const mixArray: Array<string | number | boolean> = [];

mixArray.push("rakesh");
mixArray.push(18);
mixArray.push(true);

interface User {
    name: string;
    isActive: boolean;
}

const allUsers: User[] = [];

allUsers.push({ name: "rakesh", isActive: true });

export { };